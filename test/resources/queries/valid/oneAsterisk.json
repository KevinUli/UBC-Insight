{
	"title": "SELECT dept, WHERE dept starts with c",
	"input": {
		"WHERE": {
			"IS": {
				"dataset_dept": "c*"
			}
		},
		"OPTIONS": {
			"COLUMNS": [
				"dataset_dept"
			],
			"ORDER": "dataset_dept"
		}
	},
	"errorExpected": false,
	"expected": [{
		"dataset_dept": "cpsc"
	}, {
		"dataset_dept": "cpsc"
	}]
}
